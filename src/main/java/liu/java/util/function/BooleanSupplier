-----------------------------------------BooleanSupplier-----------------------------------------------------------------------------------------------------------------------------		
BooleanSupplier：布尔值供应商。【Since:1.8】
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	表示布尔值结果的供应商（提供者）。这是原始专门化的Supplier的布尔值生产者。
	不要求每次调用供应商时都返回新的或不同的结果。
	这是一个函数接口，其函数方法是getAsBoolean()。
	
	接口定义	：@FunctionalInterface public interface BooleanSupplier
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	getAsBoolean():boolean 
		获取结果。