-----------------------------------------UnaryOperator<T>-----------------------------------------------------------------------------------------------------------------------------		
UnaryOperator<T>：一元运算器。【Since:1.8】
	<T> - 操作数和运算结果的类型
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	表示对单个操作数的操作，该操作数产生与其操作数类型相同的结果。对于操作数和结果属于同一类型的情况，这是专门化的Function。
	这是一个函数接口，其函数方法是Function.apply(Object)。
	
	接口定义	：@FunctionalInterface public interface UnaryOperator<T> extends Function<T, T>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	identity():static <T> UnaryOperator<T> 
		返回一元运算器，该运算器始终返回其输入参数。