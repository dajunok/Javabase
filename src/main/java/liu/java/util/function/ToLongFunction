-----------------------------------------ToLongFunction<T>-----------------------------------------------------------------------------------------------------------------------------		
ToLongFunction<T>：生产int值的函数。 【Since:1.8】
	<T> - 函数的输入类型
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	表示产生long值结果的函数。这是生产long值的原始专门化Function。
	这是一个功能接口，其函数方法是applyAsLong(Object)。
	
	接口定义	：@FunctionalInterface public interface ToLongFunction<T>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	applyAsLong(T value):long
		value - 函数的参数
		将此函数应用于给定的参数。