-----------------------------------------ObjDoubleConsumer<T>-----------------------------------------------------------------------------------------------------------------------------		
ObjDoubleConsumer：对象和double参数消费者。 【Since:1.8】
	<T> - 操作对象参数的类型
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	示接受Object值和double值参数的操作，不返回任何结果。这是（reference，double）专门化的BiConsumer。与大多数其他功能接口不同，ObjDoubleConsumer预期通过副作用进行操作。
	这是一个函数接口，其函数方法是accept(T t, double value)。
	
	
	接口定义	：@FunctionalInterface public interface ObjDoubleConsumer<T>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	t - 第一个输入参数
	value - 第二个输入参数
	accept(T t, double value):void 
		对给定的参数执行此操作。