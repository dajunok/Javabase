-----------------------------------------ObjLongConsumer<T>-----------------------------------------------------------------------------------------------------------------------------		
ObjLongConsumer：对象和long参数消费者。 【Since:1.8】
	<T> - 操作对象参数的类型
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	示接受Object值和int值参数的操作，不返回任何结果。这是（reference，long）专门化的BiConsumer。与大多数其他功能接口不同，ObjLongConsumer预期通过副作用进行操作。
	这是一个函数接口，其函数方法是accept(T t, long value)。
	
	
	接口定义	：@FunctionalInterface public interface ObjLongConsumer<T>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	t - 第一个输入参数
	value - 第二个输入参数
	accept(T t, long value):void 
		对给定的参数执行此操作。