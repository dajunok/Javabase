-----------------------------------------IntToLongFunction-----------------------------------------------------------------------------------------------------------------------------		
IntToLongFunction：int参数long返回值函数。 【Since:1.8】
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	表示接受int值参数并生成long值结果的函数。这是int-to-long的原始专门化Function。
	这是一个函数接口，其函数方法是applyAsLong(int)
	
	接口定义	：@FunctionalInterface public interface IntToLongFunction
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	applyAslong(int value):long 
		将此函数应用于给定的参数。