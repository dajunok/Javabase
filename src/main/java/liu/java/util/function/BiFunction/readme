一、案例说明：
	1、案例首先模拟了BiFunction函数接口（即MyiFunction）。
	2、案例在类方法定义中使用MyiFunction函数接口做为参数传入。
	3、案例在实现函数接口时（创建接口实例）同时使用匿名内部类和Lambda表达式进行对比处理分析。
	4、案例模拟定义了函数接口apply抽象方法（函数式接口只允许拥有一个抽象方法）以及default组合方法andThen（它是一个运用Lambda表达式的组合方法）。
	5、案例主要测试函数接口作为方法参数的运用。
二、源代码文件（.java）介绍：
	【MyBiFunction.java】：函数式接口，它模拟BiFunction函数接口。
	【DefBiFunction.java】：POJO,该类的方法定义中使用MyBiFunction作为参数传入。
	
	【Test.java】：案例调试类。
		1、同时使用Lambda表达式和匿名内部类方式调用DefBiFunction类中的方法。
	【TestFCompose.java】：案例调试类。
		1、测试函数接口组合方法andThen。
三、知识点：
	1、函数式接口一般在定义方法时作为参数类型传入，在具体调用时由调用者实现该接口中的方法（可以采用创建匿名内部类或者使用Lambda表达式）。
	2、函数式接口只允许定义一个抽象方法，但允许创建多个带default关键字的方法（具有方法实体代码）。
	2、组合方法：即在组合方法内部先调用第一个方法，再将第一个方法返回的值作为第二个方法的实参调用第二个方法。本案例andThen方法就是一个组合方法。
	3、Lambda表达式：整个表达式代表创建一个函数接口实例对象，该对象同时实现了函数接口唯一的抽象方法（即相当于创建了一个实现函数接口的匿名内部类）。
	4、Lambda表达式就是一个创建匿名内部类的简化形式。