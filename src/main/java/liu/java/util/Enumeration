-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
24、Enumeration<E>：枚举接口。【Since:JDK1.0】
	实现枚举接口的对象生成一系列元素，一次一个。对NextElement方法的连续调用返回序列的连续元素。目前已被迭代器Iterator<E>淘汰
	例如，打印输出Vector<E> v中的所有元素：
   		for (Enumeration<E> e = v.elements(); e.hasMoreElements();)
       		System.out.println(e.nextElement());	
	方法用于枚举vector的元素、哈希表的键和哈希表中的值。枚举还用于指定SequenceInputStream的输入流。
	注意：迭代器接口复制了枚举接口的功能。此外，迭代器添加了一个可选的移除操作，并且具有较短的方法名。新的实现应该优先考虑使用迭代器而不是枚举。
	
	
	接口定义：public interface Enumeration<E>		
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------	
	hasMoreElements():boolean 
		测试此枚举是否包含更多元素。
	nextElement():E 
		如果此枚举对象至少还要提供一个元素，则返回此枚举的下一个元素。
		【Throws】：
			● NoSuchElementException 如果不存在更多的元素。
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
附录：
	1、注：文档中的△符号表示覆盖重写方法。
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
		