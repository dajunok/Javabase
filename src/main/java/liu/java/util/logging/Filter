-----------------------------------------Filter-----------------------------------------------------------------------------------------------------------------------------		
Filter：过滤器。 【Since:1.4】
	这是一个函数接口，因此可以用作lambda表达式或方法引用的赋值目标。
	
	可以使用Filter对记录的内容提供细粒度控制，而不是日志级别提供的控制。
	每个记录器（Logger）和每个处理程序（Handler）都可以有一个与其关联的过滤器。Logger或Handler将调用isLoggable方法来检查是否应发布给定的日志记录。如果isLoggable返回false，日志记录将被丢弃。
	
	
	接口定义	：@FunctionalInterface public interface Filter
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	isLoggable(LogRecord record):boolean 
		检查是否应发布给定的日志记录。
		如果应发布日志记录，则返回true。