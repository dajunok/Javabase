----------------------------------------CRC32--------------------------------------------------------------------------------------------------------------
CRC32：	【Since:1.1】
	可用于计算数据流的CRC-32的类。
	向此类中的方法传递空参数将导致引发NullPointerException异常。
	
	
	
	类定义	：public class CRC32 implements Checksum
	实现接口	：Checksum
------------------------------------------------------------------------------------------------------------------------------------------------------
Constructor Summary（构造器概要）：
	public CRC32()
		创建新的CRC32对象。
		
Method Summary（方法概要）：
	getValue():long 
		△  返回CRC-32值。
		返回当前校验和值。
	reset():void 
		△  将CRC-32重置为初始值。
	update(byte[] b):void 
		b - 用于更新校验和的字节数组
		用指定的字节数组更新CRC-32校验和。
	update(byte[] b, int off, int len):void 
		b - 用于更新校验和的字节数组
		off - 数据的起始偏移量
		len - 用于更新的字节数
		用指定的字节数组更新CRC-32校验和。
		【Throws】：
			ArrayIndexOutOfBoundsException 如果off为负，或者len为负，或者off+len大于数组b的长度。		
	update(int b):void 
		b - 用于更新校验和的字节
		用指定的字节（参数b的低8位）更新CRC-32校验和。
	update(ByteBuffer buffer):void 	
		buffer -更新校验和的ByteBuffer 
		使用指定缓冲区中的字节更新校验和。校验和使用buffer.remaining()字节更新，从buffer.position()开始返回时，缓冲区的位置将更新到其限制；其限制将不会更改。
	
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	3、manifest文件：描述Jar文件的打包、运行信息。
	













		