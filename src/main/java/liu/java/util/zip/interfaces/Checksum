-----------------------------------------Checksum-----------------------------------------------------------------------------------------------------------------------------		
Checksum：校验和。 【Since:1.1】
	表示数据校验和的接口。	
	
	接口定义	：public interface Checksum 
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	getValue():long 
		返回当前校验和值。
	reset():void 
		将校验和重置为其初始值。
	update(byte[] b, int off, int len):void 
		b - 用于更新校验和的字节数组
		off - 数据的起始偏移量
		len - 用于更新的字节数
		用指定的字节数组更新当前校验和。
	update(int b):void 
	
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	