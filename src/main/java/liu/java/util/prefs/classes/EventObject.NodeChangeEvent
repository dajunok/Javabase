----------------------------------------NodeChangeEvent--------------------------------------------------------------------------------------------------------------
NodeChangeEvent：节点改变事件。	【Since:1.4】
	由首选项节点发出的事件，表示该节点的子节点已被添加或删除。
	注意，尽管NodeChangeEvent继承了java.util.EventObject中的可序列化接口，但并不打算将其序列化。实现了适当的序列化方法以引发NotSerializableException异常。
	
	
	类定义	：public class NodeChangeEvent extends java.util.EventObject
------------------------------------------------------------------------------------------------------------------------------------------------------
Constructor Summary（构造器概要）：
	public NodeChangeEvent(Preferences parent, Preferences child)
		parent - 添加或删除的节点的父级。
		child - 添加或删除的节点。
		构造新的NodeChangeEvent实例。

Method Summary（方法概要）：
	getChild():Preferences 
		返回添加或删除的节点。
	getParent():Preferences 
		返回添加或删除的节点的父级。
	
	
	
	
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	3、manifest文件：描述Jar文件的打包、运行信息。













		