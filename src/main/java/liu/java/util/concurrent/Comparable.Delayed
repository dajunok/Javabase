-----------------------------------------Delayed-----------------------------------------------------------------------------------------------------------------------------		
Delayed：延迟接口 【Since:1.5】
	一种混合样式接口，用于标记在给定延迟后应操作的对象。
	此接口的实现必须定义compareTo方法，该方法提供与其getDelay方法一致的顺序。

	接口定义	：public interface Delayed extends Comparable<Delayed>
	父接口	：Comparable<Delayed>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	getDelay(TimeUnit unit):long 
		以给定的时间单位返回与此对象关联的剩余延迟。返回剩余的延迟；零或负值表示延迟已经过了。
	