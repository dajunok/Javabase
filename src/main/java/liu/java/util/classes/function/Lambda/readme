一、案例说明：
二、源代码文件（.java）介绍：
三、知识点：
	1、lambda表达式使用目标类型必须是函数接口，也就是说lambda表达式只能在函数接口的方法定义中或创建函数接口实例时运用。
	2、函数接口一般在定义方法时作为参数类型传入，在具体调用时由调用者实现该接口中的方法（可以采用创建匿名类或者使用Lambda样式）。也就是说函数接口一般用在方法定义时使用。
	3、Lambda表达式只能用于创建函数接口实例，因为函数接口只有一个抽象方法，而Lambda表达式只能替代一个方法实体。Lambda表达式就是为函数式接口专设的。
	4、Lambda可以像普通方法一样拥有多条语句，用分号【;】间隔再用花括号{}括起来。
	5、整个Lambda表达式其实就是创建一个具有一个方法的匿名内部类。