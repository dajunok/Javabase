-----------------------------------------CompletionHandler<V,A>-----------------------------------------------------------------------------------------------------------------------------		
CompletionHandler<V,A>：完成处理器 【Since:1.7】
	<V> - 结果类型的I/O操作
	<A> - 附加到I/O操作的对象的类型
	使用异步I/O操作结果的处理器。
	此包中定义的异步通道允许指定完成处理器来使用异步操作的结果。当I/O操作成功完成时，调用completed方法。如果I/O操作失败，将调用failed方法。这些方法的实现应该及时完成，以避免使调用线程无法分派到其他完成处理器。
	
	

	接口定义	：public interface CompletionHandler<V,A>
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	completed(V result, A attachment):void 
		result - I/O操作的结果。
		attachment - 启动时附加到I/O操作的对象。
		操作完成时调用。
	failed(Throwable exc, A attachment):void 
		exc - 指示I/O操作失败原因的异常
		attachment - 启动时附加到I/O操作的对象。
		操作失败时调用。

		
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
		