-----------------------------------------WatchEvent<T>-----------------------------------------------------------------------------------------------------------------------------		
WatchEvent<T>：监控事件 【Since:1.7】
	<T> - 与事件关联的上下文对象的类型。
	在WatchService中注册的对象的事件或重复事件。
	事件按其类型分类，并具有一个count，以指示观察事件的次数。这样可以有效地表示重复事件。context方法返回与事件关联的任何上下文。在重复事件的情况下，所有事件的上下文都相同。	
	监视事件是不可变的，可供多个并发线程安全使用。   	
	

	接口定义	：public interface WatchEvent<T>
	嵌套接口	：Kind<T>,Modifier
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	context():T 
		返回事件的上下文；可以为null。
		在ENTRY_CREATE、ENTRY_DELETE和ENTRY_MODIFY事件的情况下，上下文是在监视服务注册的目录，与创建、删除或修改的条目之间的相对路径。		
	count():int 
		返回事件计数。如果事件计数大于1，则这是一个重复事件。
	kind():Kind<T> 
		返回事件类型。
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	