-----------------------------------------UserPrincipal-----------------------------------------------------------------------------------------------------------------------------		
UserPrincipal：用户主体。【Since:1.7】
	表示组标识的用户主体（UserPrincipal），用于确定对文件系统中对象的访问权限。组的确切定义是特定于实现的，但通常表示为管理目的创建的标识，以便确定组成员的访问权限。实体（entity）是否可以是多个组的成员，以及组是否可以嵌套，特定于实现，因此没有指定。
	
	
	接口定义	：public interface GroupPrincipal extends UserPrincipal
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	
	
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	