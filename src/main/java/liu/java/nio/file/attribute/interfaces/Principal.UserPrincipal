-----------------------------------------UserPrincipal-----------------------------------------------------------------------------------------------------------------------------		
UserPrincipal：用户主体。【Since:1.7】
	Principal表示标识的主体，用于确定对文件系统中对象的访问权限。
	在许多平台和文件系统上，实体（entity）需要适当的访问权限或权限才能访问文件系统中的对象。访问权限通常通过检查实体（entity）的标识来执行。例如，在使用访问控制列表（ACL）强制权限分离的实现上，文件系统中的文件可能具有一个关联的ACL，该ACL确定在ACL中指定的标识的访问权限。
	UserPrincipal对象是标识的抽象表示。它有一个名称，通常是它所代表的用户名或帐户名。用户主体对象可以使用UserPrincipalLookupService获取，或者由提供对标识相关属性访问的FileAttributeView实现返回。例如，AclFileAttributeView和PosixFileAttributeView提供对文件所有者的访问。
	
	
	接口定义	：public interface UserPrincipal extends Principal
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	
	
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------		
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已淘汰的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。	
	