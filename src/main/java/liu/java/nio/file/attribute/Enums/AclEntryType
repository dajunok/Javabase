-------------------------------AclEntryType-----------------------------------------------------------------------------------------------------------------------
AclEntryType：ACL条目类型。【since 1.7】
	访问控制项类型的类型安全枚举。
	
	
		
	枚举定义	：public enum AclEntryType  extends Enum<AclEntryType>
	语法糖	：enum AclEntryType
	实现接口	：Serializable, Comparable<AclEntryType>
------------------------------------------------------------------------------------------------------------------------------------------------------		
Enum Constant Summary（枚举常量概要）：
	ALARM
		以与系统相关的方式，生成在ACL条目的permissions组件中指定的访问权限的警示。
	ALLOW
		明确授予对文件或目录的访问权。
	AUDIT
		以依赖系统的方式记录在ACL条目的permissions组件中指定的访问。
	DENY	
		明确拒绝访问文件或目录。
		
Method Summary（方法概要）：
	valueOf(String name):static AclEntryType 
		name - 要返回的枚举常量的名称。
		返回具有指定名称的此类型的枚举常量。字符串必须与用于在此类型中声明枚举常量的标识符完全匹配。（不允许使用外部空白字符。）
		返回具有指定名称的枚举常量
		【Throws】：
			IllegalArgumentException 如果此枚举类型没有具有指定名称的常量
			NullPointerException 如果参数为null
	values():static AclEntryType[] 
		返回一个数组，该数组包含此枚举类型的常量，按照其声明顺序排序。此方法可用于对常量进行迭代，如下所示：
			for (AclEntryType c : AclEntryType.values())
    			System.out.println(c);		
		
			    
		
------------------------------------------------------------------------------------------------------------------------------------------------------
附录：
	1、注：文档中的△符号表示覆盖重写方法，✘符号表示已被淘汰掉的方法。	
	2、注：文档中的★符号表示抽象方法或者直接抛出UnsupportedOperationException异常的方法，总之没有实现具体功能代码的方法。
	
